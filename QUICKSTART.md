# SuperInbox 快速开始指南

## 一键启动 (推荐)

### macOS / Linux

```bash
# 使用 Bash 脚本
./start.sh
```

### Windows / macOS / Linux (跨平台)

```bash
# 使用 Node.js 脚本
node start.js
```

## 启动后会自动完成

✅ 检查 Node.js 和 npm
✅ 询问是否安装依赖 (首次运行)
✅ 检查端口占用 (3000, 3001)
✅ 交互式处理端口冲突
✅ 启动后端和前端服务
✅ 显示服务访问地址

## 服务地址

启动成功后,访问以下地址:

- **前端界面**: http://localhost:3001
- **后端 API**: http://localhost:3000/v1

## 常用命令

```bash
# 查看服务状态
./start.sh status       # Bash 版本
node start.js status    # Node.js 版本

# 停止服务
./start.sh stop         # Bash 版本
node start.js stop      # Node.js 版本

# 重启服务
./start.sh restart      # Bash 版本
node start.js restart   # Node.js 版本
```

## 端口冲突处理

如果端口被占用,启动工具会提供选项:

1. **停止占用进程** - 自动停止并继续
2. **跳过启动** - 跳过该服务,继续启动其他服务
3. **退出** - 手动处理后重试

## 自定义端口

```bash
# Bash 版本
BACKEND_PORT=8080 FRONTEND_PORT=8081 ./start.sh

# Node.js 版本
BACKEND_PORT=8080 FRONTEND_PORT=8081 node start.js
```

## 查看日志

```bash
# 实时查看后端日志
tail -f backend.log

# 实时查看前端日志
tail -f frontend.log

# 同时查看
tail -f backend.log frontend.log
```

## 故障排除

### 端口被占用无法停止?

```bash
# 查找占用端口的进程
lsof -i :3000

# 手动停止
kill -9 <PID>
```

### 依赖安装失败?

```bash
# 单独安装
cd backend && npm install
cd ../web && npm install
```

### 需要更多帮助?

查看完整文档: [启动工具使用说明.md](./启动工具使用说明.md)

## 下一步

- 📖 阅读 [项目背景.md](./项目背景.md) 了解项目定位
- 🏗️ 查看 [技术架构说明书.md](./技术架构说明书.md) 了解架构设计
- 📝 参考 [SuperInbox-Core-API文档.md](./SuperInbox-Core-API文档.md) 了解 API 接口
